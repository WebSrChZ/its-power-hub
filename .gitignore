tools/
.claude/
*.log
*.tmp
node_modules/

# Variáveis de ambiente (contém credenciais)
.env

# Binários e documentos sensíveis
*.pdf
*.doc
*.docx
*.pptx

# Server antigo (morto — site é estático via GitHub Pages)
server.js
package.json
package-lock.json

# Pastas de produção (mídia pesada, não pertence ao repo)
Audiovisual*/
Cronograma*/

# Workspace local de edição de vídeo (mídia pesada + artefatos — não pertence ao repo do cliente)
videos/
__pycache__/
